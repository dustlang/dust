[package]
name = "fallible-iterator"
version = "0.3.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Minimal local shim for offline build resolution"

[lib]
path = "src/lib.rs"

[features]
default = ["std"]
std = []
